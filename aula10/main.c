#include <stdio.h>
// também serve para definir uma constante, mas não é muito recomendado
// #define qtd 3;

void main() {
    // definindo um array de tamanho 3 do tipo float, cada valor será do tipo float
    // const int qtd = 3;
    // float array[3];
    
    // float array[qtd];
    // float media = 0;

    // for (int i = 0; i < qtd; i++) {
    //     printf("\nDigite 1 numero: ");
    //     scanf(" %f", &array[i]);
    //     media += array[i];
    // }

    // media /= qtd;
    // printf("A media dos numeros eh: %.2f", media);

    // outra maneira de preencher os valores do array
    // int array2[] = {1, 4, 15, 19};

    // ====================================================================
    //array, sequencia de variaveis
    //string, sequencia de variavesi do tipo char
    //vetor, sequencia de variaveis sem tamanho definido
}